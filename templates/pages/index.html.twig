{% extends "base.html.twig" %}

{% block title %}
    Accueil -
    {{parent()}}
{% endblock %}

{% block body %}
    <section id="hero" class="d-flex align-items-center justify-content-center container-fluid py-3 py-md-5 text-center catchphrase-image">
        <div class="card col-md-8 p-lg-8 mx-auto p-5 mb-3 container catchphrase-background">
            <h1 class="display-4 fw-normal text-white">Bienvenue sur SnowTricks !</h1>
            <p class="lead fw-normal text-white">Ici vous pouvez partager votre passion de la neige et vos tricks pour contribuer à une communauté active et soudée. N'attendez plus, rejoignez-nous !</p>
        </div>
        <a href="#tricks" class="scroll-btn scroll-down"><em class="fas fa-arrow-circle-down"></em></a>
    </section>
    <section id="tricks" class="d-flex flex-row flex-wrap justify-content-around container mt-4 pt-3 pb-5 text-center">
        <h3 class="display-5 col-12 fw-normal mb-4"><em class="fas fa-snowboarding text-primary me-3"></em>Derniers tricks</h3>
        <div class="d-flex flex-row flex-wrap justify-content-around container text-center">
        {% for trick in tricks %}
                <a href="/trick/{{ trick.slug }}" class="mb-3 col-sm-12 col-md-3 px-2 trick">
                    <article class="card bg-light d-flex flex-row flex-wrap card-body p-2 shadow-sm">
                        <img class="col-12 rounded px-0" src="{{ asset('img/default_trick.jpg') }}" alt="Trick" />
                        <h5 class="card-title text-dark px-0 mt-1 m-0 fw-bold"><em class="fas fa-chevron-right text-primary me-1"></em>{{ trick.name }}</h5>
                        <div class="ms-auto pt-1">
                            <em class="fas fa-edit text-warning"></em>
                            <em class="fas fa-trash-alt text-danger"></em>
                        </div>
                    </article>
                </a>
        {% endfor %}
        </div>
        {% if remain_tricks == true %}
        <button class="btn btn-primary" id="load-more" data-element="trick">
            <em class="fas fa-spinner"></em><br />Charger plus de tricks
        </button>
        {% endif %}
    </section>
    <a href="#hero" class="scroll-btn scroll-up text-primary"><em class="fas fa-arrow-circle-up"></em></a>


{% endblock %}
